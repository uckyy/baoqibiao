name: entry_board
description: Entry board application.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.1.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  azlistview: any
  contained_tab_bar_view: any
  file_selector: any
  flutter_ime: any
  font_awesome_flutter: any
  wakelock_plus: any
  http: ^1.1.0
  file_selector_windows: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
